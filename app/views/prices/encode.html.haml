.page-header
  %h1 Precio codificado

.image
  %img{ src: "/encode.svg?price[company]=#{@price.company}&price[store]=#{@price.store}&price[product]=#{@price.product}&price[value]=#{@price.value}" }
.text
  %h1= number_to_currency @price.value
  %h4= @price.product
  %h4= "#{@price.company} #{@price.store}"

.row-fluid
  .span12
    .form-actions
      = link_to 'Nuevo código QR', '/new', class: 'btn btn-primary'
      = link_to 'Simular escaneo', "/submit?c=#{@price.company}&s=#{@price.store}&p=#{@price.product}&v=#{@price.value}", class: 'btn'
